import React from 'react';
import {SafeAreaView, Text} from 'react-native';

export function SeatMapScreen(): React.JSX.Element {
  return (
    <SafeAreaView style={{padding: 16}}>
      <Text>SeatMapScreen Placeholder</Text>
    </SafeAreaView>
  );
}
