import React from 'react';
import {SafeAreaView, Text} from 'react-native';

export function ShowListScreen(): React.JSX.Element {
  return (
    <SafeAreaView style={{padding: 16}}>
      <Text>ShowListScreen Placeholder</Text>
    </SafeAreaView>
  );
}
